apiVersion: v1
kind: Service
metadata:
  name: {{ include "aar.fullname" . }}-service
spec:
  selector:
    app.kubernetes.io/name: {{ include "aar.fullname" . }}
  ports:
    - protocol: TCP
      port: {{.Values.service.port}}
      targetPort: {{.Values.image.port}}